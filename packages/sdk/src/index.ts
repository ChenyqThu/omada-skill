// @omada/sdk — typed client for the Omada Open API.
//
// Re-exports the generated operations map and (from M1-06 onward) the
// OmadaClient + auth + middleware layer.

export * from "./generated/operations.js";
